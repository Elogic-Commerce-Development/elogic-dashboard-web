# Supabase — copy to .env.local and paste real values.
# Anon key is safe to ship in the bundle; RLS is the security boundary.
VITE_SUPABASE_URL=https://<project-ref>.supabase.co
VITE_SUPABASE_ANON_KEY=<anon-public-key>
